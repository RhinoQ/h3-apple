#!/bin/bash
set -euo pipefail
root="$(cd "$(dirname "$0")" && pwd)"
python="$root/.local/envs/h3/bin/python"
if [[ ! -x "$python" ]]; then
  echo 'Run ./install.sh first.' >&2
  exit 1
fi
exec "$python" -I -m h3_apple "$@"
